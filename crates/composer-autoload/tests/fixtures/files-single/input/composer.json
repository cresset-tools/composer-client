{
    "name": "fixture/files-single",
    "description": "Single package exposing a `files` autoload entry.",
    "type": "project",
    "repositories": [
        { "type": "path", "url": "packages/helpers", "options": { "symlink": false } }
    ],
    "require": {
        "acme/helpers": "*"
    }
}
